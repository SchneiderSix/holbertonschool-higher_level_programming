#!/usr/bin/python3
"""
Module is_same_class
"""


def is_same_class(obj, a_class):
    """aWrite a function that returns True if the object is exactly an instance of the specified class ; otherwise False.
    a
    a
    a
    a
    """
    return issubclass(a_class, type(obj))
